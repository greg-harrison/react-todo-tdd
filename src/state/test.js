import enzymify from 'expect-enzyme';
import expect from 'expect';

import { store } from './store';

expect.extend(enzymify());

describe('Store', () => {
  // it('fetches records from an input', () => {
  // })
});
